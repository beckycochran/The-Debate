import styled from "styled-components";

const Header = () => {
    return (
        <Wrapper>
            Header
        </Wrapper>
    )
}

const Wrapper = styled.div`
  display: flex;
`
export default Header;